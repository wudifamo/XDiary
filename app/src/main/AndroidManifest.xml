<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.k.xdiary">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/Theme.AppStartLoadTranslucent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.city.AddCityActivity">
        </activity>
        <activity android:name=".ui.weight.WeightActivity">
        </activity>
        <activity android:name=".ui.weight.AddWeightActivity">
        </activity>
        <activity android:name=".ui.diary.DiaryActivity">
        </activity>
        <activity android:name=".ui.diary.AddDiaryActivity">
        </activity>
    </application>

</manifest>